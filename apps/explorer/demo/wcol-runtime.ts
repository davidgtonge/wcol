/** Type-only surface for demo code that must not load Wasm on the main thread. */
export type { QueryPlan, QueryResult } from "./query/plan-types.ts";
export type { ByteSource } from "./worker/effects.handlers.ts";
