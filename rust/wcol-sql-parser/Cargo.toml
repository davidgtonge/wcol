[package]
name = "wcol-sql-parser"
version = "0.1.0"
edition = "2021"
description = "Minimal SQL parser for wcol query dialect (0 deps, wasm-friendly)"
readme = "readme.md"

[lib]
path = "src/lib.rs"

[dependencies]

[dev-dependencies]
